@{
    ViewBag.Title = "Conexão Oeste";
}

@model PortalConexao.Models.HomeIndexViewModel

@if (Model.Noticias.Length == 0)
{
    <div class="row">
        <span> Não há noticias para serem exibidas</span>
    </div>
}
else
{
    <div class="row container-fluid" style="margin-top: 60px">

        <div class="bd-example" style="margin-left: 10px">
            <div id="carouselExampleCaptions" class="carousel slide" data-ride="carousel">
                <ol class="carousel-indicators">
                    <li data-target="#carouselExampleCaptions" data-slide-to="0" class="active"></li>
                    <li data-target="#carouselExampleCaptions" data-slide-to="1"></li>
                    <li data-target="#carouselExampleCaptions" data-slide-to="2"></li>
                </ol>
                <div class="carousel-inner img-fluid">
                    <div class="carousel-item active">
                        <a href="/Home\DetalheCarousel\4">
                            <img src="\imagens\cat.jpg" class="d-block w-100 img-fluid size-carousel">
                            <div class="carousel-caption d-none d-md-block">
                                <h5 class="font-card">Barueri realiza feira especial de adoção responsável dia 30</h5>
                            </div>
                            </a>
                    </div>
                    @foreach (var ca in Model.Carousels)
                    {

                        <div class="carousel-item img-fluid">
                            <a href="/Home/DetalheCarousel/@ca.CarouselId">
                                <img src="@ca.Imagem" class="d-block w-100 img-fluid size-carousel " alt="...">
                                <div class="carousel-caption d-none d-md-block">
                                    <h5 class="font-card">@ca.Titulo</h5>
                                    <p class="font-card">@ca.SubTitulo</p>
                                </div>
                            </a>
                        </div>

                    }

                </div>
                <a class="carousel-control-prev" href="#carouselExampleCaptions" role="button" data-slide="prev">
                    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                    <span class="sr-only">Previous</span>
                </a>
                <a class="carousel-control-next" href="#carouselExampleCaptions" role="button" data-slide="next">
                    <span class="carousel-control-next-icon" aria-hidden="true"></span>
                    <span class="sr-only">Next</span>
                </a>
            </div>
        </div>



        @foreach (var item in Model.Noticias)
        {
            <div class="card espacos" style="width: 18rem; margin-top: 50px">

                <a href="/Home/NoticiaDetalhe/@item.Id">
                    <img src="@item.Imagem" class="card-img-top" alt="Imagens Noticias">
                    <div class="card-body">
                        <h5 class="card-title title-card">@item.Titulo</h5>
                        <p style="font-size: 12px;">@item.DataPublicacao</p>
                        <p class="card-text body-card">@item.SubTitulo</p>

                    </div>
                </a>
            </div>
        }
    </div>
}
